import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.tabladeidiomas.starter',
  appName: 'tabladeidiomas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
